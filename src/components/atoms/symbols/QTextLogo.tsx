import React, { FC } from "react";

type QTextLogoProps = {
};

export const QTextLogo: FC<QTextLogoProps> = (props) => {
    const {
    } = props;

    return (
        <div >
        </div>
    );
};
