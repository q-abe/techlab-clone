import React, { FC } from "react";

type SerachIconProps = {
};

export const SerachIcon: FC<SerachIconProps> = (props) => {
    const {
    } = props;

    return (
        <div >
        </div>
    );
};
