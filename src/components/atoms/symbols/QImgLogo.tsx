import React, { FC } from "react";

type QImgLogoProps = {
};

export const QImgLogo: FC<QImgLogoProps> = (props) => {
    const {
    } = props;

    return (
        <div >
        </div>
    );
};
