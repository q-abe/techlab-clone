import React, { FC } from "react";

type SearchBarProps = {
};

export const SearchBar: FC<SearchBarProps> = (props) => {
    const {
    } = props;

    return (
        <div >
        </div>
    );
};
