import React, { FC } from "react";

type ArticleCardProps = {
};

export const ArticleCard: FC<ArticleCardProps> = (props) => {
    const {
    } = props;

    return (
        <div >
        </div>
    );
};
