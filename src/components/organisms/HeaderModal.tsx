import React, { FC } from "react";

type HeaderModalProps = {
    label: string;
};

export const HeaderModal: FC<HeaderModalProps> = (props) => {
    const {
    } = props;

    return (
        <div >
        </div>
    );
};
